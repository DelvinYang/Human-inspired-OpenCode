# Data Directory

This directory stores repository demo inputs.

Versioned data in this repository includes `data/demo/ind/`,
and `data/demo/longtail_cases/`. Full-data runs write generated tensors,
reference-path outputs, caches, checkpoints, and paper-scale outputs under
the repository-root `results/` directory.

Configure local dataset paths under `code/configs/datasets/` for full-data runs.
