dataset_root: /path/to/raw_dataset_root
cache_root: artifacts/reference_paths_work/cache/scenes
work_dir: artifacts/reference_paths_work
supported_datasets:
  - HighD
  - inD
  - CitySim
  - sinD
  - NGSIM
  - DJI
notes:
  - "dataset_root is a local top-level directory containing raw dataset folders."
  - "Reference-path outputs are derived metadata and should be checked against each dataset license before redistribution."
